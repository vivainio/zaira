---
paths:
  - "zaira/skills/**"
---

After editing this file, bump its `updated: YYYY-MM-DD` frontmatter via `markstate` — don't hand-edit it (see AGENTS.md's "Skill Files" section):

```bash
markstate update <path-to-file> --set updated=today
```
